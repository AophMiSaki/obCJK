# obCJK_iniEdit 使用說明

本檔說明 `obCJK_iniEdit.py`（字體/外掛設定編輯GUI）介面上各個欄位、選單、開關的意義，
方便對照 `obCJK.ini` 內容或調整效果時查閱。工具分成「字型」跟「選項」兩個分頁。

---

## 一、字型頁

### 1. 編碼 / 語言選擇
左上角可切換目前正在編輯的編碼：`BIG5`（繁中）/ `GBK`（簡中）/ `SJIS`（日文）/
`KOREAN`（韓文）/ `UTF8`（萬國碼）。五種編碼共用同一份
`obCJK.dll`，各自的字體參數分別存在 ini 的 `[BIG5]`/`[GBK]`/`[SJIS]`/`[KOREAN]`/`[UTF8]` 節。

同一列右側還有「顯示語言」（見二-3）跟「NorthernUI 相容」下拉選單，對應寫入
`[obCJK]` 節的 `NorthernUIEnable` 鍵（0/1/2）：
- **原版/其他UI**（`NorthernUIEnable=0`，預設）：不啟用 NorthernUI 橋接。
- **原NorthernUI**（`=1`）：官方未修改過的 `NorthernUI.dll`，obCJK.dll 執行期
  直接讀取該DLL內 FontInfo+0x08 的 rawID 取得字型角色資訊。
- **新NorthernUI**（`=2`）：使用者自行重新編譯過的 patch 版 `NorthernUI.dll`，
  改透過 OBSE Messaging 廣播跟 obCJK.dll 溝通字型角色資訊。

選其中一個非「原版/其他UI」的選項後，字型分頁會多開一排分頁：鎖住的 SLOT9/10
佔位分頁，跟 5 個 NorthernUI 角色分頁，詳見「四、NorthernUI 字型角色分頁」。

字型頁、選項頁的內容如果超出視窗高度，會各自變成可垂直捲動的區域（滑鼠移進
去後可用滾輪捲動），底部「儲存 ini」列固定在捲動範圍外。

### 2. CJK字體 / ASCII字體（半角）
左側字體清單上方的兩顆單選鈕，切換「編輯CJK自選字型」或「編輯半形英數字元自選字型」，
兩者是各自獨立的一套 SLOT 參數。

### 3. SLOT（字體使用位置）
- **SLOT 1**：標題、大部分UI、書籍
- **SLOT 2**：HUD、對話字幕
- **SLOT 3**：地圖地名、彈出文本、剩餘UI…
- **SLOT 5**：書籍、信件（手寫風格）
- **SLOT 7**：MenuQue 額外字型 1（僅遊戲內啟動且偵測到 MenuQue 載入額外字型時才可編輯）
- **SLOT 8**：MenuQue 額外字型 2（同上）

每個 SLOT 分頁內有「半角」「全角」兩列，各自可填字型名稱＋以下參數。
可參考:https://oblivionjapanize.wordpress.com/2022/06/29/font_settings/

### 4. NorthernUI 字型角色分頁
只有「NorthernUI 相容」下拉選單（見1）選到「原NorthernUI」或「新NorthernUI」時，
字型頁右側才會多出第二排分頁：一個鎖住的 SLOT9/10 佔位分頁（保留未來擴充，目前
完全沒有串接任何字型參數），跟緊接在後的 5 個 NorthernUI 角色分頁：

- **Normal**
- **Large**
- **MediumLargeUpper**
- **Shadowed**
- **Small**

這 5 個角色名稱是 NorthernUI 本身在 `Data\Menus\NorthernUI\datastore.xml` 內定義的
字型特徵（`_fontNormal`/`_fontLarge`/`_fontMediumLargeUpper`/`_fontShadowed`/
`_fontSmall`），不是 obCJK 自行分類的UI位置；哪個角色實際對應遊戲畫面上的哪個元素，
取決於使用者安裝的 NorthernUI XML 介面本身怎麼指定，obCJK 只是原樣沿用這5個角色
名稱，並不重新定義。

每個角色分頁的介面跟一般 SLOT 分頁完全相同（半形/全形兩列＋完整的「5. 字型參數」
跟「套用選取」/「初始化」按鈕），初始預設值沿用 SLOT 1 的內建預設值。寫入的 ini
鍵依序是 `FontParam33`～`FontParam37`（Normal=33、Large=34、MediumLargeUpper=35、
Shadowed=36、Small=37），跟原生 SLOT1/2/3/5/7/8 對應 `FontParam1`/`FontParam2`/
`FontParam3`/`FontParam5`/`FontParam7`/`FontParam8` 是同一套鍵名規則，只是編號往
後延伸到 33～37。

這 5 個分頁一律可編輯，不像 SLOT9/10 佔位分頁那樣鎖住——即使該角色目前在
datastore.xml 沒有定義、或對應的字型檔實際不存在（此時 obCJK.dll 端該角色的橋接
會保持未啟用，log 會印出對應警告），iniEdit 介面上仍可正常調整並儲存該角色的
參數，只是遊戲內暫時看不出效果，等日後該角色被啟用時參數才會生效。

「原NorthernUI」（`=1`）跟「新NorthernUI」（`=2`）兩種模式只影響 obCJK.dll 執行期
怎麼偵測/取得這 5 個角色實際對應的字型物件（見1的說明），iniEdit 這邊的角色分頁
介面跟參數儲存方式兩種模式完全相同，不需要因為選了哪個模式而改變操作方式。

### 5. 字型參數（每個 SLOT/NorthernUI 角色分頁的半角/全角列皆有）

- **字寬**：調整每個字所佔的水平格寬（單位：像素px）。填0代表沿用字型本身量出的自然
  寬度；填正值時每個字都會被撐滿/壓縮成該像素寬的格子（不含字距）。
- **高**：調整字的整體大小（單位：像素px，即GDI字型的lfHeight）。
- **字距**：調整字與字之間的額外間距（單位：像素px）。可填負值讓字排得更緊密，正值
  則加大字間空隙。
- **Y偏移**：調整字形垂直方向的位移量（單位：像素px）。**正值往上偏移、負值往下移**、
  0＝原位，實際是加在字形繪製原點（glyph origin）上，並非絕對座標。
- **濃度**：調整字身的黑/淡程度（範圍：-8～+8）。數值越大字看起來越黑越粗，數值越小
  字看起來越淡越細。
- **粗細**：字體粗細（可選 300細 / 400標準 / 500適中 / 700粗體，即GDI字重）。
- **對比**：調整字身顏色的對比程度（可選 弱 / 普通 / 強烈）。
- **斜體**：勾選後字形以斜體樣式繪製。

每列右側有「套用選取」（把目前字體清單選取的字型名稱套用到這一列）跟「初始化」（還原
成該編碼/該SLOT的內建預設值）兩顆按鈕。

### 6. 造字背景不透明度
範圍0～100（單位：百分比%）。0＝完全透明背景，100＝全黑底，中間值為半透明黑底。
只影響造字時的背景合成方式，不影響字身本身的濃度/對比。

### 7. 標點/窄字水平對齊
只影響「黑盒比cell格窄」的字（標點符號、日文小假名等），一般全形漢字不受影響：
- **規則A（依字型bearing）**：沿用字型本身設計的左邊界位置（多數CJK字型的標點/小假名
  是貼左下的）。
- **規則B（置中）**：不管字型bearing，強制把字的黑盒置中在整個cell格內，對所有窄字生效。
- **規則C（指定標點強制置中）**：只有 `BIG5`/`GBK`/`UTF8` 編碼才有這個選項（`SJIS`/
  `KOREAN` 目前沒有驗證過的標點清單，暫不開放）。跟規則B的差別是只對「規則C標點
  清單」內指定的字強制置中，清單外的窄字仍走規則A的邏輯。選規則C時下方會多出
  「規則C標點清單：」一列，右側是唯讀預覽欄＋「設定」按鈕，點開會跳出表格編輯視窗
  可增刪清單裡的字（預設6個：。，、；：·）。這個選項是給覺得規則B「全部窄字都置中」
  太過頭、但又想讓某幾個特定符號置中的人用。

### 8. 預覽
下方畫布即時顯示目前選取字型套用以上參數後的樣式（僅為近似預覽，非遊戲內實際渲染）。

---

## 二、選項頁

### 1. 熱鍵設定與 IME 模式
最上面兩列是跟「哪個按鍵綁哪個動作」無關的共用設定：
- **手把熱鍵偵測**：手把熱鍵總開關，預設關閉。關閉時，不論下面「手把長按鍵」
  「手把組合鍵」是否個別勾選啟用，手把熱鍵一律不會生效；只有勾選這個總開關，
  手把熱鍵才會真的被偵測。扳機鍵不支援直接指定為熱鍵（不同手把的類比軸差異太大，
  偵測不穩定），若要用扳機觸發熱鍵，請改用 JoyToKey 等工具先把扳機映射成手把按鍵
  或鍵盤按鍵。
- **IME 模式**：**外部視窗（原有）**呼叫系統原生的外部輸入法視窗；**內部輸入**在
  遊戲畫面內直接顯示輸入介面，不跳出外部視窗。

下面「編輯器」「IME」各自一個框，每個框內都是同一套 3 組**互不排斥、可同時獨立
生效**的綁定（不再是舊版「鍵盤/手把三選一」的裝置下拉選單）：
- **鍵盤**：按鍵＋修飾鍵（無／Ctrl／Shift）。「開啟編輯器」預設 Ctrl+F12
  （單獨 F12 是 Steam 內建截圖鍵）；「開啟 IME」預設 F11（DIK_F11＝0x57，無修飾鍵）。
  設定鍵盤按鍵時若跟系統/Steam 常見快捷鍵衝突（如 Ctrl+C/V/S、Shift+Tab 等），
  會跳出提示視窗告知衝突對象，但仍可以繼續使用。
- **手把長按鍵**：單一手把按鍵按住超過自訂秒數（0.1～10.0秒，預設1.0秒）才觸發。
- **手把組合鍵**：左邊修飾鍵（只開放肩鍵 LB/RB、Back(Menu)、Start 四個按鍵，避免
  誤選到 A/B/X/Y 這種常單獨使用的按鍵）＋右邊主鍵（不開放左右搖桿按鍵）。

每一列（鍵盤／手把長按鍵／手把組合鍵）左側都有各自的「停用」勾選框，只灰化那一列
自己的控制項，不影響其他列；框內最下方另有「全部停用」總勾選框，勾選後上面 3 個
停用勾選框變成不可選取（但保留原本勾選狀態）。手把長按鍵、手把組合鍵預設是停用的，
只有鍵盤綁定預設生效。按「設定」後依裝置按下對應鍵盤按鍵或手把按鈕即可捕捉；手把
偵測不到裝置時會在對話框顯示錯誤提示。

### 2. 功能開關
由上到下依序：
- **ASCII 應用自選字型**：開啟後半形英數字元套用自訂字型；關閉則半形字沿用遊戲原生
  字型（不受本工具「ASCII字體」分頁參數影響）。
- **啟用 MenuQue Hook**：CJK分隔字元誤判修正，用於戰利品清單等 MenuQue 顯示的文字。
  `UTF8` 編碼下 C++ 端結構性不需要此修正，切到 UTF8 時這個開關會被鎖住（不可調整）。
- **啟用 LootMenu 修正**：戰利品清單 SLOT 7/8 亂碼修正。此開關依附於「啟用 MenuQue
  Hook」，需先開啟 MenuQue 才能調整（`UTF8` 編碼下同樣鎖住）。
- **PATH A 斷行"-"改成空白**：長字串強制換行時，原版引擎會在斷行處插入連字號"-"
  標記，開啟後改用空白取代，關閉則維持原版"-"。只影響 Path A（選單按鈕文字的強制
  換行）。BIG5/GBK/SJIS/KOREAN/UTF8 皆適用，預設開啟。
- **PATH B、C 斷行"-"改成空白**：同上概念但作用在 Path B（控制台/選單本文）與
  Path C（書籍/卷軸）共用的排版函式，兩者在原生引擎中未被拆分，因此只有一個共用
  開關（無法分開設定 B、C）。預設開啟。
- **取消CJK全部顯示**：除錯用一鍵開關，開啟後CJK與ASCII自選字型全部停用，整個退回
  遊戲原生字型顯示效果，方便比對「有沒有套用自選字型」的差異。
- **顯示debug log**：關閉時log只顯示各項Hook是否成功套用；開啟後才會額外印出詳細
  診斷內容。
- **PATH A/B/C 診斷log**：開啟後印出CJK字型替換過程中的catch（成功套用）/miss（退回
  原生字型）訊息，右側下拉選單為每條路徑各自的印出行數上限（300/500/1000三選一）；
  勾選「只印 slot7/8」時，只會印出 SLOT 7/8（MenuQue額外字型）相關的診斷行，其餘SLOT
  的命中不佔用行數上限。
- **slot7/8 修正診斷log**：開啟後印出戰利品清單 SLOT 7/8 亂碼修正的命中訊息，右側下拉
  選單同樣是印出行數上限（300/500/1000）。
- **凍結監控（Debug）**：介面卡住超過5秒會自動把當下所有執行緒的呼叫堆疊寫入
  `obCJK_iniEdit_hang.log`，純粹是本工具自身（iniEdit.py）用來排查難重現卡死問題的
  除錯功能，`obCJK.dll` 端不會讀取這個設定。
- **存檔/刪檔完整記錄log（Debug）**：關閉時每次存檔/刪檔動作只印出成功或失敗一行
  （失敗附簡短原因）；開啟後印出完整診斷內容，包含背景存檔清單掃描的逐筆診斷行。
- **手把輸入檢測（Debug）**：開啟後每幀掃描手把全部按鍵並把「剛按下」的動作記錄到
  log（第一行會先記錄目前偵測到的是哪支手把），跟「手把熱鍵偵測」開關是否開啟無關，
  用來在設定手把熱鍵之前先確認手把本身有沒有被正確辨識。

### 3. 顯示語言
右上角可切換本工具**操作介面**（按鈕/標籤文字）的顯示語言：繁體中文／簡體中文／日文／
韓文。這跟「編碼/語言選擇」（正在編輯哪個編碼的字體參數）是兩件獨立的事，互不影響。

---

## 三、儲存

畫面下方「儲存 ini」按鈕會把目前所有設定（不分頁面）一次寫回同一份 `obCJK.ini`
（UTF-8編碼）。若填寫的字型名稱在系統字型清單中找不到，儲存前會跳出確認提示，仍可
選擇繼續儲存。
